# Ailog API Configuration
# Used by /rag-eval for benchmarking against Ailog's production RAG

# Your Ailog API key (get one at https://app.ailog.fr/settings/api-keys)
# Must have 'api' scope for developer API access
AILOG_API_KEY=pk_live_your_api_key_here

# Your workspace ID (find it in workspace settings)
AILOG_WORKSPACE_ID=123

# Optional: Custom API endpoint (default: https://api.ailog.fr)
# AILOG_API_URL=https://api.ailog.fr

# Usage:
# 1. Copy this file to .env in your project root
# 2. Fill in your actual values
# 3. Run /rag-eval with --benchmark-ailog flag
